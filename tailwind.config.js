/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      // your custom colors, fonts, etc. if any
    },
  },
  plugins: [],
  // You can remove darkMode line — it's not used in v4 CSS-first approach
}